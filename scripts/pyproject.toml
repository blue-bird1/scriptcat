[project]
name = "scriptcat-build-tools"
version = "0.1.0"
description = "Remote build and portable activation tooling for ScriptCat MCP"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
